# Oral English Practice · 5 分钟装机指南

*[中文 README](README.zh-CN.md) · [English](README.md) · 本页 = 手把手装机指南*

> 一套装在你自己电脑上的英语口语长期训练工具。
> **你在 Claude 手机/桌面 App 用语音开口练 + 练听力,Claude Code(命令行版)帮你
> 记录每次的逐字错题、打分、分析趋势、定下次重点。**
> 所有数据只在你电脑里。

---

## 装之前确认你有这些

| 你需要 | 干啥用 | 没有怎么办 |
|---|---|---|
| ✅ Windows 或 Mac 电脑 | 跑这套工具 | — |
| ✅ Claude Code（命令行版 / IDE 扩展） | 记录 + 分析(这套 skill 在这里跑) | https://claude.com/code 下载 + 订阅 |
| ✅ Claude App（手机或桌面） | 语音开口练习 | App Store / 官网下载 |

> 这套是"两件套":**App 练嘴(语音),Claude Code 记账(skill)**。两个都要有。
>
> 好消息:**一份 Claude 订阅(Pro/Max)就同时覆盖 App 和 Claude Code**,不用买两次。
> 所以整套是「**一次订阅 + 每次两下粘贴(报告进来、重点出去)= 长期练习**」。

> **要装别的依赖吗?不用。** 上面两件套就能完整使用——记录、错题银行、备份、诊断全靠
> Claude Code 自己完成,零额外安装。
> 唯一**可选**项:想要**趋势折线图**,需要电脑上有 Python + matplotlib;**没有也照常用**,
> 趋势会以文字版呈现,想要图随时 `pip install matplotlib` 即可。

---

## 第 1 步：放好这个文件夹

解压后是这样：

```
Oral-English-Practice/
├── oral-english-practice/      ← 真正的 skill（会被复制到 Claude 能找到的地方）
│   ├── SKILL.md                ← 引擎主文件
│   ├── references/             ← 数据格式与规则
│   ├── scripts/                ← 画趋势图 + 写前备份
│   └── templates/              ← 首次使用时用来生成你的数据文件
├── install.bat                 ← Windows 装机脚本
├── install.sh                  ← Mac/Linux 装机脚本
├── README.md                   ← 详细说明
└── 【先看这里】.md             ← 本文件
```

把整个文件夹放到一个你不会随手删的地方（比如 `D:\` 或桌面）。**源文件夹别删**,以后更新可能用。

---

## 第 2 步：跑装机脚本（30 秒）

**Windows**：双击 `install.bat`,看到 `[DONE] skill copied`,按回车关掉。

**Mac / Linux**：终端 `cd` 到这个文件夹,跑 `bash install.sh`。

它做的事：把 `oral-english-practice` 复制进 `~/.claude/skills/`。**不碰任何数据,重复跑也安全。**

> 想手动装也行:直接把 `oral-english-practice` 文件夹拷进
> `C:\Users\你的用户名\.claude\skills\`（Mac 是 `~/.claude/skills/`）即可。

---

## 第 3 步：开始用

### ① 要练习时 —— 在 Claude Code 里说：

```
/oral-english-practice
```
或者大白话「我要练口语」。它会给你一段 **app-prompt**(还有上次的重点)。

### ② 去 Claude App 练 —— 语音模式

1. 新建一个对话,**先在文字框**把 app-prompt 粘贴进去、发送(第一次没有"下次重点"可跳过)
2. 等它回复后,**点语音模式**按钮,开口聊 15-30 分钟(对话 + 听力)
3. 练完它会给你 **标注实录 + 评估报告 + DATA BLOCK**

### ③ 回 Claude Code 记录 —— 把报告整份贴回去

把 App 给的那一大份(含最底下 `date=... | cefr=...` 那行)复制,粘回 Claude Code。
skill 会自动：
- 存进你的错题本 `transcripts.md`
- 入库 `data.csv`(趋势数据)
- 存完整报告到 `sessions/`
- 更新 `next-focus.md`(下次重点)
- 给你一段中文诊断

### ④ 想看进步 —— 说「看趋势」

练够 5 次以上,在 Claude Code 说「看趋势」或「复盘」,它读数据给你画图 + 解读。

---

## 你的练习记录在哪

默认在 `C:\Users\你的用户名\oral-english-practice-log\`（Mac 是
`~/oral-english-practice-log/`),**首次练习记录时自动创建**。skill 第一次建好
这个文件夹时,会**直接把完整路径打印给你**,所以你一定知道记录存在哪。
里面就是你所有的练习数据(逐次报告、错题本、成绩趋势)。

**想把记录放到别处（比如 D 盘、某个网盘同步夹）?可以,两种方式:**

- **最省事**:直接跟你的 Claude 说一句,比如
  「把我的练习记录改放到 `D:\英语\` 去」。它会帮你设置好,并把已有记录一起搬过去。
- **自己动手**:在家目录建一个名为 `.oral-english-data-path` 的文件
  (注意:**点开头、没有后缀**),里面写一行你想要的路径
  (如 `D:\oral-english-practice-log`),保存。下次 skill 就用这个新位置。

> 改不改都行——不改就用默认的家目录位置,完全够用。

---

## 隐私说明

- **你的记录只在你电脑里。** 这个 skill **没有任何联网代码**——不上传、不回传、
  无遥测、无云同步。你的数据(成绩、错题、报告、备份)只写在本地数据目录,
  `.gitignore` 还保证它们不会被提交到 git。
- **但用它要通过 Claude(云端 AI)。** 练习和处理离不开 Claude Code 和 Claude App,
  所以你实际处理的内容(贴进去的报告、语音对话)会作为**正常的 Claude 使用**经过
  Anthropic 服务器——和你平时跟 Claude 聊天完全一样,不是 skill 在额外传数据。
- **没有第三方。** 除了你本来就在用的 Anthropic,数据不去任何别处,也**没有**你练习
  档案的云端副本。

---

## 出问题了？

| 现象 | 解决 |
|---|---|
| Claude Code 里 `/oral-english-practice` 找不到 | 跑 `/help` 看有没有;或重启 Claude Code;确认装到了 `~/.claude/skills/oral-english-practice/SKILL.md` |
| App 语音模式粘贴长文本没反应 | 语音模式不能贴长文本;先在普通文字框发提示词,再切语音 |
| 报告贴回去 skill 没正确入库 | 确认报告**整份**复制了,尤其最底下 DATA BLOCK 那一行 |
| 趋势图画不出来 | matplotlib 是**可选**的,没装也正常;让 Claude 给文字版趋势,或想要图就 `pip install matplotlib` |

---

## 这套是可以按你口味改的

教练的语气、话题、纠错风格**不是写死的**——它们存在你自己的 `app-prompt.md`
里(在你的数据目录下),改它只影响你一个人,不动 skill 本体。两种改法:

**① 最省事:直接跟你的 Claude 说**(跟它聊天就行,它会帮你改文件)
- 「把教练弄温和一点 / 严厉一点」
- 「话题多来点科学、历史、哲学,别老聊生活」
- 「纠错狠一点,每个错都给我指出来」
- 「语速快一点,练我的听力」
- 「今天专门练面试场景的对话」

**② 自己动手**:用记事本打开数据目录里的 `app-prompt.md`,改代码块里的文字。

| 能放心改的 | 改了要注意的 |
|---|---|
| 教练语气(温和/严厉) | **打分维度**(加项/改名)——要同时改 `data.csv` 表头和报告里的 DATA BLOCK 那行,否则记录会乱 |
| 话题范围、难度 | |
| 纠错强度、听力语速 | |
| 练习时长、场景 | |

> 默认给你的是「温和鼓励 + 宽话题(像托福那样什么都聊)」,先用着,觉得哪不对随时调。

---

## 想知道更多

完整说明、所有细节 → [`README.md`](./README.md)

祝练得开心 ✨
