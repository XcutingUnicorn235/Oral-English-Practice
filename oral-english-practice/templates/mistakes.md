# Mistake Bank

Confirmed recurring errors, kept so they get re-drilled until fixed. A pattern
enters once it shows up in **≥2 sessions** (or **≥3 times in one session**);
it's marked `resolved` after **2 consecutive clean sessions** (archived, not
deleted). The skill maintains this table from each session's transcript. See
`references/data-format.md` for the exact rules.

| id | pattern | example (wrong → right) | tag | count | first_seen | last_seen | clean_streak | status |
|----|---------|-------------------------|-----|-------|------------|-----------|--------------|--------|
| (no confirmed patterns yet — needs ≥2 sessions) |
